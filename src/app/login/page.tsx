import Link from 'next/link'
import { Logo } from '@/components/logo'
import { InputField } from '@/components/field'
import { signIn } from '@/app/actions'
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;message?:string;next?:string}>}){const q=await searchParams;const next=q.next?.startsWith('/')&&!q.next.startsWith('//')?q.next:'';return <main className="grid min-h-screen place-items-center px-5"><div className="w-full max-w-md"><div className="mb-8 flex justify-center"><Logo/></div><section className="np-card p-7"><h1 className="text-2xl font-black tracking-tight">Bem-vindo de volta</h1><p className="mt-2 text-sm text-[#607269]">Acesse sua operação NutriPro.</p>{q.error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{q.error}</p>}{q.message&&<p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-800">{q.message}</p>}<form action={signIn} className="mt-6 grid gap-4">{next&&<input type="hidden" name="next" value={next}/>}<InputField label="E-mail" name="email" type="email" autoComplete="email" required/><InputField label="Senha" name="password" type="password" autoComplete="current-password" required/><button className="np-button mt-1">Entrar</button></form><div className="mt-5 flex justify-between text-sm"><Link className="font-semibold text-[#167451]" href="/recuperar-senha">Esqueci a senha</Link><Link className="font-semibold text-[#167451]" href={`/cadastro${next?`?next=${encodeURIComponent(next)}`:''}`}>Criar conta</Link></div></section></div></main>}
