import { importPatientsCsv } from './actions'
import { Feedback, PageHeader } from '@/components/module-ui'
import { requirePermission } from '@/lib/auth'

export default async function ImportPage({searchParams}:{searchParams:Promise<{message?:string;error?:string}>}){const sp=await searchParams;const {supabase,organizationId}=await requirePermission('patient.read');const {data:imports}=await supabase.from('patient_imports').select('*').eq('organization_id',organizationId).order('created_at',{ascending:false}).limit(30);return <div className="mx-auto max-w-5xl pb-20"><PageHeader eyebrow="Dados" title="Importar pacientes" description="A importação é transacional: uma falha de banco reverte o lote inteiro; linhas inválidas de validação ficam registradas sem virar pacientes."/><Feedback {...sp}/><section className="np-card mt-7 p-6"><h2 className="font-bold">Enviar CSV</h2><p className="mt-2 text-sm leading-6 text-[#607269]">Coluna obrigatória: <code>nome</code>. Opcionais: <code>nome_social, nascimento, cpf, telefone, whatsapp, email, objetivo, status</code>. Até 5.000 linhas e 2 MB.</p><form action={importPatientsCsv} className="mt-5 flex flex-wrap items-end gap-3"><label className="min-w-0 flex-1"><span className="np-label">Arquivo CSV UTF-8</span><input className="np-input" name="file" type="file" accept=".csv,text/csv,text/plain" required/></label><button className="np-button">Validar e importar</button></form></section><section className="np-card mt-5 p-6"><h2 className="font-bold">Histórico</h2>{imports?.length?<div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="text-xs uppercase text-[#718179]"><th className="py-2">Arquivo</th><th>Total</th><th>Válidas</th><th>Inválidas</th><th>Importadas</th><th>Status</th></tr></thead><tbody>{imports.map(item=><tr className="border-t border-[#edf2ef]" key={item.id}><td className="py-3 font-bold">{item.file_name}</td><td>{item.total_rows}</td><td>{item.valid_rows}</td><td>{item.invalid_rows}</td><td>{item.imported_rows}</td><td><span className="np-badge">{item.status}</span></td></tr>)}</tbody></table></div>:<p className="mt-4 text-sm text-[#607269]">Nenhuma importação executada.</p>}</section></div>}
