import Link from 'next/link'; import { createPatient } from '@/app/actions'; import { PatientForm } from '@/components/patient-form'; import { requireOrganization } from '@/lib/auth'
export default async function NovoPaciente({searchParams}:{searchParams:Promise<{error?:string}>}){ await requireOrganization(); const q=await searchParams; return <div className="mx-auto max-w-5xl pb-20"><Link href="/app/pacientes" className="text-sm font-bold text-[#167451]">← Pacientes</Link><h1 className="mt-4 text-3xl font-black">Novo paciente</h1><p className="mt-2 text-sm text-[#607269]">Comece pelo necessário. Dados clínicos ficam em módulos separados e com permissões mais restritas.</p>{q.error&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-sm text-red-700">Não foi possível salvar. Revise os campos.</p>}<div className="mt-7"><PatientForm action={createPatient}/></div></div> }
